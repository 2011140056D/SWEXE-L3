<h1>Tweets#index</h1>

<p><%= link_to "Tweet","/tweets/new" %></p>
=============================
<% @tweets.each do |tweet| %>
    <p>
        <%= tweet.message %>
        <%= (Time.current.min - tweet.tdate.min).abs %>分前
        <br>
        <%= link_to "削除", "/tweets/#{tweet.id}", method: :delete %>
        <%= link_to "表示", "/tweets/#{tweet.id}" %>
        <%= link_to "編集", "/tweets/#{tweet.id}/edit" %>
    </p>
    =============================
<% end %>
